---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: reboots.rebooto.work
spec:
  group: rebooto.work
  names:
    kind: Reboot
    listKind: RebootList
    plural: reboots
    singular: reboot
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Reboot is the Schema for the reboots API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RebootSpec defines the desired state of Reboot.
            properties:
              delayBetweenRestarts:
                description: Delay between latest triggered restart
                format: date-time
                type: string
              gracePeriodSeconds:
                description: Delay before restart (rollout) triggered
                format: int64
                type: integer
              matchLabels:
                additionalProperties:
                  type: string
                description: Labels selector for the resources to restart during reconcile
                type: object
              targetNamespaces:
                description: Target namespaces where to watch for the targets
                items:
                  type: string
                type: array
              targetResources:
                description: The name of the target resource to watch (configmap/secret)
                items:
                  type: string
                type: array
            required:
            - gracePeriodSeconds
            - matchLabels
            - targetResources
            type: object
          status:
            description: RebootStatus defines the observed state of Reboot.
            properties:
              lastRestartTime:
                description: Track last triggered restart time
                properties:
                  nanos:
                    description: |-
                      Non-negative fractions of a second at nanosecond resolution. Negative
                      second values with fractions must still have non-negative nanos values
                      that count forward in time. Must be from 0 to 999,999,999
                      inclusive. This field may be limited in precision depending on context.
                    format: int32
                    type: integer
                  seconds:
                    description: |-
                      Represents seconds of UTC time since Unix epoch
                      1970-01-01T00:00:00Z. Must be from 0001-01-01T00:00:00Z to
                      9999-12-31T23:59:59Z inclusive.
                    format: int64
                    type: integer
                required:
                - nanos
                - seconds
                type: object
              restartCount:
                description: Restart count
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
